---
name: Hello

on:
  push:
    branches: [ main ]

    jobs:
      greet:
        runs-on: ubuntu-latest
        steps:
          -uses: actions/checkout@v4

          - name: Greet the world
            run: echo "Hello, World!"