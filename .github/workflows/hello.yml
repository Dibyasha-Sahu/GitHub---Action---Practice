name: Hello

on:
  push:
    branches: [main]

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:

      - name: Greet the world
        run: echo "Hello, world!"

      - name: Print Date and Time
        run: date

      - name: List Files
        run: ls -la    